# ---- 采集产物：每日变动、体积大，不入库 ----
# gold.db 约 7MB、features.csv 约 2MB、market.csv 约 900KB，
# 都是脚本可重新生成的中间数据，提交进来只会让仓库膨胀且每天产生无意义 diff。
data/
*.db

# ---- 作品集 PDF 与打印中间件：由 make_pdf.py 生成 ----
# 每次运行都会重新生成（数字随每日数据变化），属于构建产物。
# 需要投递用的 PDF 时，在本地运行 scripts/make_pdf.py 即可。
黄金走势预测工作台_作品集.pdf
docs/portfolio-print.html

# ---- Python ----
__pycache__/
*.py[cod]
.venv/
venv/

# ---- macOS ----
.DS_Store
._*

# ---- 编辑器 ----
.vscode/
.idea/
.Rhistory

# ---- AI 助手工作记忆（属于本地协作过程，非交付物）----
# 如需把「与 AI 协作的过程记录」也作为作品集的一部分展示，删掉下面这行即可
.workbuddy/
