# 仓库根目录 .gitignore —— 只忽略「系统垃圾」和「编译产物」
# 注意：这里不做任何删除操作，被忽略的文件仍完整保留在本地磁盘上。

# ---- macOS 系统文件 ----
# Finder 自动生成，记录文件夹图标位置/窗口大小/排序方式。
# 与项目无关，且每次打开文件夹都可能变动，会造成无意义的 git 改动和跨机器冲突。
# 不带斜杠的写法可匹配任意层级目录下的同名文件。
.DS_Store
._*
.Spotlight-V100
.Trashes

# ---- 编译产物（可重新生成，体积巨大）----
# DesktopCat 的 Swift 编译输出：226MB，由 swift build 重新生成。
# 万一误用 `git add .` 会造成灾难性提交，这里做个保险。
.build/
*.o
*.dSYM/

# ---- Python ----
__pycache__/
*.py[cod]
.venv/
venv/

# ---- 编辑器 ----
.vscode/
.idea/
.Rhistory
